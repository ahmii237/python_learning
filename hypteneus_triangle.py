import math
height=float(input("Enter the height of triangle: "))
base = float (input("enter the base of triangle: "))

hypoteneus= math.sqrt(pow(height,2)+pow(base,2))

print(f"The hypteneus of the triangle is {hypoteneus}")